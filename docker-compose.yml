services:
  control-center:
    build: .
    container_name: control-center
    restart: unless-stopped
    ports:
      - "8099:8099"
    env_file:
      - .env
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.prod
    volumes:
      - ./data:/app/data
      - .:/app
      - /var/run/docker.sock:/var/run/docker.sock
    command: >
      sh -c "python manage.py migrate &&
             gunicorn config.wsgi:application --bind 0.0.0.0:8099 --workers 1 --timeout 120"
